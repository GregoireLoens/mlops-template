# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environnements & secrets
.venv/
.env

# Outils
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Données : le contenu est versionné par DVC, pas par git.
# Les pointeurs *.dvc générés par `dvc add` restent commités.
data/*
!data/.gitkeep
!data/*.dvc
!data/README.md
/reports/data_docs
# BP3 : logs d'inférence locaux + rapports de drift (artefacts régénérables).
/data/inferences
/reports/monitoring

# MLflow local (sans serveur)
mlruns/
/models
mlflow.db

# Dagster local : homes temporaires régénérables à chaque exécution
.tmp_dagster_home*/
